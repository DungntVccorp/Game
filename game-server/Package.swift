// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "game-server",
    targets: [
        .target(
            name: "game-server",
            dependencies: []),
    ]
)
